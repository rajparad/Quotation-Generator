import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'rajparad_FrontEnd',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
